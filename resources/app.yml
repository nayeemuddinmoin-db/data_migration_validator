resources:
  apps:
    data_migration_validator_app:
      name: "dmv-app-v2-nayeem"
      source_code_path: ../src/validation/migration-data-validation/databricks_apps/streamlit-data-migration-validator
      description: "A Streamlit app that lets users trigger the job included in this bundle."

      resources:
        - name: "app-job"
          description: "A job that can be invoked by this app."
          job:
            id: ${resources.jobs.wf_dmv_validator_runner.id}
            permission: "CAN_MANAGE_RUN"
